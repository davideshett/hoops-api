{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "//Postgres": "Uses the shared local 'infrastructure' Postgres on 5432, database 'hoops'. Override with the ConnectionStrings__Postgres environment variable.",
    "Postgres": "Host=localhost;Port=5432;Database=hoops;Username=postgres;Password=postgres"
  },
  "Jwt": {
    "Issuer": "hoops-api",
    "Audience": "hoops-clients",
    "//SigningKey": "DEV ONLY. Override in production via the Jwt__SigningKey environment variable or a secrets manager.",
    "SigningKey": "dev-only-insecure-signing-key-change-me-0123456789abcdef",
    "AccessTokenMinutes": 60,
    "RefreshTokenDays": 14
  }
}
