{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "//Postgres": "Uses the shared local 'infrastructure' Postgres on 5432, database 'hoops'. Override with the ConnectionStrings__Postgres environment variable.",
    "Postgres": "Host=localhost;Port=5432;Database=hoops;Username=postgres;Password=postgres"
  },
  "Jwt": {
    "Issuer": "hoops-api",
    "Audience": "hoops-clients",
    "//SigningKey": "Deliberately absent. Supplied from a secret store via Jwt__SigningKey; Development substitutes an in-memory placeholder. Startup fails fast if it is missing, short, or the placeholder.",
    "AccessTokenMinutes": 60,
    "RefreshTokenDays": 14
  }
}
