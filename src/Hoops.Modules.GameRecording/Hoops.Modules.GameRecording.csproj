<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Hoops.SharedKernel\Hoops.SharedKernel.csproj" />
    <!-- Contracts only: GameRecording triggers a recompute on finalisation. Statistics does NOT
         reference GameRecording, so the dependency stays one-directional. -->
    <ProjectReference Include="..\Hoops.Modules.Statistics\Hoops.Modules.Statistics.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
